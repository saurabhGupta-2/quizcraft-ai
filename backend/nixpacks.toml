[phases.setup]
nixPkgs = ["python311", "gcc", "g++"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements/prod.txt"]

[phases.build]
cmds = ["mkdir -p /app/data/chromadb"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
